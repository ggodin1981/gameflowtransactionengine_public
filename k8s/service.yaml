apiVersion: v1
kind: Service
metadata:
  name: gameflow-api
spec:
  selector:
    app: gameflow-api
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: gameflow-signalr
spec:
  selector:
    app: gameflow-signalr
  ports:
    - port: 80
      targetPort: 8080
